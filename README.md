# ephemeral.team

Source for the [ephemeral.team](https://www.ephemeral.team) product information site.

Static HTML/CSS, deployed via GitHub Pages from the `main` branch.

## Local preview

```bash
python3 -m http.server 8000
```

Then open http://localhost:8000
